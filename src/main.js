import { Character } from './../js/character';
import { Item } from './../js/item';
import { Battle, roll } from './../js/battle';
import './styles.css';

$(document).ready(function() {
  let newCharacter = new Character();
});
